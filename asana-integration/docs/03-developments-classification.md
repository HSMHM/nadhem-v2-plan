# 03 — تصنيف الـ19 تطويراً (إضافة / تعديل)

تم التحقق من كل تصنيف بفحص فعلي للكود في `x:/laragon/www/saas-events-frontend/`. هذا الملف هو **مصدر الحقيقة** لكل من:
- سكربت `generate-fad-jsons.js` (يحدد قيمة `request_type` و `_section`)
- سكربت `push-to-asana.js` (يحدد القسم في أسانا — `إضافات` أو `التعديلات`)

---

## جدول التصنيف الكامل

| # | ID | العنوان | quarter | تصنيف | request_type | الواجهات المتأثرة |
|---|---|---------|---------|-------|--------------|--------------------|
| 1 | 101 | توحيد نماذج إنشاء الحسابات + فئات متغيرة | P0 | **تعديل** | `change` | admin + user + api |
| 2 | 102 | إعادة تصميم واجهات admin + ثيمين خارجيين | P0 | **تعديل** | `change` | admin + public + api |
| 3 | 103 | إعادة هيكلة الجلسات + إلغاء الشراكات | P0 | **تعديل** | `change` | admin + api |
| 4 | 104 | تطوير التقارير ولوحة المعلومات | P0 | **تعديل** | `change` | admin + api |
| 5 | 1 | نظام الوحدات والباقات (16 وحدة) | P1 | **إضافة** | `new` | admin + user + public + api |
| 6 | 2 | إعادة هيكلة الخصائص لكل لجنة v2 | P1 | **تعديل** | `change` | admin + api |
| 7 | 3 | تطوير خاصية خارطة المعرض | P2 | **تعديل** | `change` | admin + user + api |
| 8 | 4 | تمكين التوقيع الإلكتروني | P2 | **إضافة** | `new` | admin + user + api |
| 9 | 5 | التكامل مع النفاذ الوطني | P3 | **إضافة** | `new` | admin + user + public + api |
| 10 | 6 | قوالب جاهزة مصممة مسبقاً للأقسام v2 | P2 | **إضافة** | `new` | admin + public + api |
| 11 | 7 | بناء الأقسام بالسحب والإفلات | P3 | **إضافة** | `new` | admin + public + api |
| 12 | 8 | التكامل مع أجهزة الخدمات الذاتية (Kiosks) | P4 | **إضافة** | `new` | admin + api (+ Kiosk UI) |
| 13 | 9 | تطبيق الهواتف الذكية | P4 | **إضافة** | `new` | api (+ mobile app) |
| 14 | 10 | تقارير مؤشرات الأداء اللحظية v2 | P3 | **تعديل** | `change` | admin + api |
| 15 | 11 | دمج وتوحيد بنية اللجان v2 | P1 | **تعديل** | `change` | admin + api |
| 16 | 12 | تطوير خاصية إرسال الدعوات التفاعلية | P3 | **تعديل** | `change` | admin + user + api |
| 17 | 13 | الخصومات والخدمات في اللجنة اللوجستية | P2 | **إضافة** | `new` | admin + public + api |
| 18 | 14 | ربط الذكاء الاصطناعي | P4 | **إضافة** | `new` | admin + api (+ AI provider) |
| 19 | 15 | تطبيق التصميم الموحد v2 | P4 | **إضافة** | `new` | admin + user + public |

**الإجمالي:** 9 تعديلات | 10 إضافات

---

## التفاصيل والتبرير لكل تطوير

### DEV-101 — توحيد نماذج إنشاء الحسابات + فئات متغيرة 🔧 تعديل
**التصنيف:** `change`
**التبرير:**
- النماذج موجودة فعلياً: `routes/web.php` → `/register/{type}` يدعم 8 أدوار حالياً
- الخدمات: AuthService, AttendeeService, UserService, ScientificUserController موجودة
- UserTypeEnum و ParticipantRoleEnum معرّفان
**الخصائص الموجودة المتأثرة:** S02 (التسجيل والأدوار) — F01, F02, F03, F04
**ما الجديد في التطوير:** توحيد الأدوار العلمية تحت "المشاركين" + فئات حضور متغيرة (سعر/عنوان/وصف)

---

### DEV-102 — إعادة تصميم admin + ثيمين خارجيين 🔧 تعديل
**التصنيف:** `change`
**التبرير:**
- واجهات Admin موجودة: `resources/js/Admin/` SPA كامل + `resources/views/admin/`
- ملفات ثيمات أساسية موجودة: `theme.dark.css`, `theme.unrounded.css` (لكن بدون آلية تبديل ديناميكي)
- البنية الأساسية لـ Theme Engine جزئية
**الخصائص الموجودة المتأثرة:** S13 (اللجان الإدارية الثمانية) + S07 (المحتوى) + كل واجهات Admin
**ما الجديد:** إعادة تصميم بصري كامل + بناء Theme Engine ديناميكي + ثيمين خارجيين

---

### DEV-103 — إعادة هيكلة الجلسات + إلغاء الشراكات + منصة التوقيع 🔧 تعديل
**التصنيف:** `change`
**التبرير:**
- الجلسات موجودة في اللجنة العلمية: permission `scientific-event-places-list` موجود
- اللجنة التنظيمية موجودة: `OrganizationCommitteeRoutes.js`
- لجنة الشراكات موجودة: `PartnershipCommitteeRoutes.js` (مقرر إلغاؤها)
- اللجنة الإعلامية موجودة: `InformativeCommitteeRoutes.js`
- نماذج الرعاية والمعارض والشراكات موجودة (كلها ستُجمع في "منصة التوقيع")
**الخصائص الموجودة المتأثرة:** S01 (إدارة الفعاليات), S03 (المعارض), S04 (الرعايات), S13 (لجنة الشراكات → سيلغى, اللجنة الإعلامية)
**ما الجديد:** نقل + إلغاء + بطاقة موحدة جديدة "منصة التوقيع"

---

### DEV-104 — تطوير التقارير ولوحة المعلومات 🔧 تعديل
**التصنيف:** `change`
**التبرير:**
- DashboardController موجود ولديه views
- تقارير الحضور والبطاقات موجودة بشكل أساسي
- لكن لا توجد رسوم بيانية متقدّمة أو تقارير محسّنة
**الخصائص الموجودة المتأثرة:** S10 (حساب المستخدم — DashboardController) + S13 (لجان متعددة لها تقاريرها)
**ما الجديد:** تحسين تقارير وإضافة رسوم بيانية مفصّلة

---

### DEV-1 — نظام الوحدات والباقات (16 وحدة) ➕ إضافة
**التصنيف:** `new`
**التبرير:** **لا توجد** أي بنية للوحدات/الباقات/تفعيل الخصائص حسب الاشتراك
- `SponsorPlan` و `ExhibitionPlan` موجودان لكنهما خطط رعاية/معرض (مفهوم مختلف)
- لا يوجد ModulePackage أو SubscriptionTier أو feature-gating
**ما الجديد كلياً:**
- جدول DB جديد للوحدات
- جدول DB جديد للباقات
- ربط الوحدات بالمنظمين (TenantModule)
- صفحة مقارنة الباقات
- آلية حماية تمنع الوصول للخصائص غير المفعّلة
- تعديل القائمة الجانبية لإخفاء الوحدات غير المفعّلة

---

### DEV-2 — إعادة هيكلة الخصائص لكل لجنة v2 🔧 تعديل
**التصنيف:** `change`
**التبرير:** كل اللجان الثمانية موجودة في Admin SPA — هذا تطوير لإعادة ترتيبها وتنظيمها
**الخصائص الموجودة المتأثرة:** S13 (كل اللجان الثمانية)
**ما الجديد:** تنظيم/إعادة ترتيب فقط

---

### DEV-3 — تطوير خاصية خارطة المعرض 🔧 تعديل
**التصنيف:** `change`
**التبرير:**
- `DrawPlanExhibition.js` موجود في الكود (يستخدم Konva.js لرسم المساحات)
- `ExhibitionPlanService.php` موجود
- لكن الرسم الحالي بدائي — تطوير 3 يحوّله إلى تجربة تفاعلية كاملة مع الحجز والدفع
**الخصائص الموجودة المتأثرة:** S03 (المعارض) — F03 (خطط المعرض)
**ما الجديد:** أنواع مساحات + حالات (متاحة/محجوزة/...) + الحجز والدفع المباشر من الخريطة

---

### DEV-4 — تمكين التوقيع الإلكتروني ➕ إضافة
**التصنيف:** `new`
**التبرير:**
- يوجد رفع صورة توقيع (في ExhibitionContractSettingsView)
- لكن **لا يوجد** سير عمل توقيع إلكتروني فعلي (إرسال للتوقيع → التوقيع → الأرشفة)
- لا تكامل مع مزود توقيع إلكتروني خارجي
**ما الجديد كلياً:** سير العمل بالكامل + تكامل مزود + الأرشيف

---

### DEV-5 — التكامل مع النفاذ الوطني ➕ إضافة
**التصنيف:** `new`
**التبرير:**
- بنية SocialLogin عامة موجودة (SocialProviderService) لكن **بدون** مزود نفاذ
- يحتاج تسجيل في بوابة النفاذ + تكامل OAuth/SAML خاص
**ما الجديد:** مزود النفاذ + ربط الحسابات الموجودة + UI جديد

---

### DEV-6 — قوالب جاهزة للأقسام v2 ➕ إضافة
**التصنيف:** `new`
**التبرير:**
- `image-templates.service.js` و `mail-template.service.js` موجودان (لكن لمواضيع مختلفة)
- **لا توجد** مكتبة قوالب صفحات ولا بنية لاختيار القالب لقسم
**ما الجديد كلياً:** نظام القوالب + 10-15 قالب بصري + UI الاختيار

---

### DEV-7 — بناء الأقسام بالسحب والإفلات ➕ إضافة
**التصنيف:** `new`
**التبرير:**
- مكتبة `vuedraggable` مثبتة (تُستخدم في أماكن متفرّقة)
- **لا توجد** صفحة بناء للترتيب الديناميكي للأقسام
**ما الجديد:** Page Builder كامل + المعاينة + حفظ الترتيب

---

### DEV-8 — التكامل مع أجهزة Kiosks ➕ إضافة
**التصنيف:** `new`
**التبرير:** **لا توجد** أي ملفات تتعلق بـ kiosk أو الطباعة الذاتية
**ما الجديد كلياً:** API للأجهزة + UI شاشة الترحيب + لوحة مراقبة الأجهزة

---

### DEV-9 — تطبيق الهواتف الذكية ➕ إضافة
**التصنيف:** `new`
**التبرير:** **لا يوجد** مشروع React Native ولا Flutter في الـ repo
**ما الجديد كلياً:** مشروع Mobile App منفصل + API mobile-friendly + Push notifications

---

### DEV-10 — تقارير v2 (لحظية + ختامية) 🔧 تعديل
**التصنيف:** `change`
**التبرير:** يبني على DashboardController الموجود ويوسعه
**ما الجديد:** WebSocket للتحديث اللحظي + تقرير ختامي PDF + تصدير Excel

---

### DEV-11 — دمج وتوحيد بنية اللجان v2 🔧 تعديل
**التصنيف:** `change`
**التبرير:** يكمل DEV-103 (دمج لجنة الشراكات في الإعلامية)
**ما الجديد:** ضبط التسميات الرسمية فقط

---

### DEV-12 — الدعوات التفاعلية 🔧 تعديل
**التصنيف:** `change`
**التبرير:**
- `LogisticInviteesView.vue` موجود (إدارة المدعوين)
- `LogisticCommitteeRoutes.js` لديه CRUD للدعوات
- **لا يوجد** نظام RSVP تفاعلي حالياً
**ما الجديد على الموجود:** صفحة استجابة المدعو + حالات الدعوة + إشعارات لحظية

---

### DEV-13 — الخصومات والخدمات (الفنادق/الطيران) ➕ إضافة
**التصنيف:** `new`
**التبرير:** **لا توجد** أي بنية لخصومات الفنادق أو الطيران
- `CouponService` موجود لكنه لخصومات تذاكر الفعالية فقط
**ما الجديد كلياً:** قسم جديد كامل في اللوجستية + صفحة عامة "عروض وخصومات"

---

### DEV-14 — ربط الذكاء الاصطناعي ➕ إضافة
**التصنيف:** `new`
**التبرير:** **لا يوجد** أي تكامل مع OpenAI/GPT أو chatbot
**ما الجديد كلياً:** مزود AI + قاعدة معرفة + اقتراح الجدول + UI المساعد

---

### DEV-15 — Design System v2 ➕ إضافة
**التصنيف:** `new`
**التبرير:** **لا توجد** مكتبة مكونات رسمية (Storybook، component-library)
- مكونات Vue موجودة ومتفرّقة عبر `Admin/components` و `common/`
**ما الجديد:** مكتبة موحّدة + التوكنز + Storybook + تطبيق تدريجي

---

## كيف يستخدم السكربت هذا الملف

`generate-fad-jsons.js` يحوي **مصفوفة JS صريحة** بنفس البيانات أعلاه (لتجنب parsing markdown):

```js
const CLASSIFICATION = {
  101: { type: 'change', section: 'mod', interfaces: ['admin','user','api'] },
  102: { type: 'change', section: 'mod', interfaces: ['admin','public','api'] },
  103: { type: 'change', section: 'mod', interfaces: ['admin','api'] },
  104: { type: 'change', section: 'mod', interfaces: ['admin','api'] },
  1:   { type: 'new',    section: 'add', interfaces: ['admin','user','public','api'] },
  2:   { type: 'change', section: 'mod', interfaces: ['admin','api'] },
  3:   { type: 'change', section: 'mod', interfaces: ['admin','user','api'] },
  4:   { type: 'new',    section: 'add', interfaces: ['admin','user','api'] },
  5:   { type: 'new',    section: 'add', interfaces: ['admin','user','public','api'] },
  6:   { type: 'new',    section: 'add', interfaces: ['admin','public','api'] },
  7:   { type: 'new',    section: 'add', interfaces: ['admin','public','api'] },
  8:   { type: 'new',    section: 'add', interfaces: ['admin','api'] },
  9:   { type: 'new',    section: 'add', interfaces: ['api'] },
  10:  { type: 'change', section: 'mod', interfaces: ['admin','api'] },
  11:  { type: 'change', section: 'mod', interfaces: ['admin','api'] },
  12:  { type: 'change', section: 'mod', interfaces: ['admin','user','api'] },
  13:  { type: 'new',    section: 'add', interfaces: ['admin','public','api'] },
  14:  { type: 'new',    section: 'add', interfaces: ['admin','api'] },
  15:  { type: 'new',    section: 'add', interfaces: ['admin','user','public'] },
};
```

`section: 'mod'` → SECTION_MOD_GID (التعديلات)
`section: 'add'` → SECTION_ADD_GID (إضافات)
